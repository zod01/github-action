name: ENV_DEMO
on: workflow_dispatch
env:
  APP_ENV: "dev"

jobs:
  env_demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: evn_test
        env:
          APP_ENV: "prod"
        run: echo "APP connecting to $APP_ENV"
